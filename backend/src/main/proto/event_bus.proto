syntax = "proto3";

package social.example.eventbus.grpc;

option java_package = "social.example.eventbus.grpc";
option java_outer_classname = "EventBusProto";
option java_multiple_files = true;

import "google/protobuf/empty.proto";
import "timeline.proto";
import "likes.proto";
import "posts.proto";

service EventBusService {
  rpc EventBus (EventBusRequest) returns (stream Event);
  rpc Subscribe (SubscribeRequest) returns (google.protobuf.Empty);
  rpc Unsubscribe (UnsubscribeRequest) returns (google.protobuf.Empty);
}

message ConnectionContext {
  string id = 1;
  int64 epoch = 2;
}

message EventBusRequest {
  ConnectionContext context = 1;
  repeated Subscription subscriptions = 2;
}

message SubscribeRequest {
  ConnectionContext context = 1;
  Subscription subscription = 2;
}

message Subscription {
  string subscription_id = 1;
  oneof request {
    social.example.features.timeline.grpc.SubscribeTimelineRequest timeline = 2;
    social.example.features.posts.grpc.SubscribePostRequest post = 3;
    social.example.features.likes.grpc.SubscribeLikesRequest likes = 4;
  }
}

message Event {
  string subscription_id = 1;
  oneof response {
    social.example.features.timeline.grpc.SubscribeTimelineResponse timeline = 2;
    social.example.features.posts.grpc.SubscribePostResponse post = 3;
    social.example.features.likes.grpc.SubscribeLikesResponse likes = 4;
  }
}

message UnsubscribeRequest {
  ConnectionContext context = 1;
  string subscription_id = 2;
}
